-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
# Carga las variables de entorno de pytest.ini antes de importar la app.
pytest-env>=1.1.0
# TestClient de FastAPI/Starlette.
httpx>=0.27.0
# Fijado para que CI y tu máquina reporten exactamente los mismos avisos.
ruff==0.16.1
